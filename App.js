import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native/types_generated/index';

export default function App() {
  return (
    <SafeAreaView>
      <View>
        <Text>Aplicação primeiro cp</Text>
      </View>
    </SafeAreaView>
  );
}
